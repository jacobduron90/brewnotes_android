<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.brewnotes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".framework.BrewApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".coffeebag.CoffeeBagActivity"
            android:label="@string/title_activity_coffee_bag"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".coffeebag.CoffeeBagDetailActivity"
            android:label="@string/title_activity_coffee_bag_detail"
            android:theme="@style/AppTheme" />
        <activity android:name=".dashboard.DashboardActivity" />
        <activity android:name=".recommendation.RecommendationSummaryActivity"></activity>
    </application>

</manifest>